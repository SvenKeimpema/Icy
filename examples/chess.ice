#print chess board
let x = 0
let y = 0

while x < 8:
    x = x + 1
    while y < 8:
        y = y + 1

        if(x + y > 32)
            #TODO add BINOP voor print
            print(x)
            print(y)
        end
    end
end